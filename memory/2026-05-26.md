# 2026-05-26

- Switched the default OpenRouter model in both news scripts and GitHub Actions workflow fallbacks from `openrouter/free` to `google/gemma-4-26b-a4b-it:free` at the user's request.
- Confirmed the requested model is currently listed as free on OpenRouter's official model page before applying the change.
- Investigated repeated `HTTP 429: Provider returned error` responses from the Gemma free endpoint. OpenRouter documents lower/variable availability for free variants, a 20 requests/minute limit, and 50 free requests/day unless the account has purchased at least 10 credits.
- Changed per-news summary generation to one batch request per push and configured OpenRouter's `models` fallback array to try Gemma first, then `openrouter/free`, reducing normal API usage from roughly one request per news item to one request per run.
