- Investigated Daily News summary failures. GitHub Actions logs showed OpenRouter returning HTTP 404 because `qwen/qwen3-32b:free` is unavailable for free, and the existing `models` array request did not fall through to the fallback models. Updated `scripts/fetch-and-push.js` to try OpenRouter models one by one and add `openrouter/free` as the final fallback; also made JSON parsing tolerant of extra text around the array.
