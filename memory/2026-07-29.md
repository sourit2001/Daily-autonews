- 第一电动快讯页不再更新。已将 `scripts/fetch-and-push.js` 的新闻源替换为盖世汽车资讯列表 `https://i.gasgoo.com/news/c-0-1.html`。
- 盖世汽车列表使用 `.contentFile > ul > li`，可提取标题、详情链接、简介和发布日期；详情正文使用 `.technologyContent`，精确发布时间位于 `.introduce span`。
- 2026-07-29 实测 Node.js HTTPS 返回 HTTP 200，首页解析出 10 条新闻；列表简介若以省略号截断，会继续抓取详情正文。
- 飞书卡片底部的数据来源改为根据本次新闻动态生成，不再固定显示“第一电动”。
- 用户发现只抓列表第1页会漏掉仍在24小时内的后续页新闻。已改为自动分页（默认最多20页），因盖世分页并非始终严格按时间排序，连续两页明显超过48小时候选窗口才停止；最终仍以详情页精确发布时间过滤24小时。
- 修复 `parseDate` 丢弃时分秒的问题：站点时间按北京时间解析，避免把前一天晚间、实际仍在24小时内的新闻误判为过期。
