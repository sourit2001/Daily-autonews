# 2026-05-24

- Investigated the OpenRouter migration after AI summaries appeared to stop working.
- Confirmed the May 22 migration changed both scripts and GitHub workflows from `DEEPSEEK_API_KEY` to `OPENROUTER_API_KEY`, using `deepseek/deepseek-v4-flash:free`.
- OpenRouter's official model pages currently list DeepSeek V4 Flash (free) as available; the official resilient free endpoint is `openrouter/free`.
- Updated `scripts/fetch-and-push.js`, `scripts/daily-summary.js`, and the workflows to default to `openrouter/free`, permit an `OPENROUTER_MODEL` repository variable override, and log HTTP/API/timeout failures instead of silently hiding LLM errors.
- Added startup and per-summary success logs for `fetch-and-push.js`, including the actual model OpenRouter reports when the free router succeeds.
- Verified both modified JavaScript files with `node --check` and checked the diff for whitespace errors. No live API or Feishu request was sent during diagnosis.
